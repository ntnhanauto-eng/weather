CREATE TABLE IF NOT EXISTS `users` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `username` VARCHAR(50) NOT NULL UNIQUE,
  `password` VARCHAR(100) NOT NULL,
  `fullname` VARCHAR(100),
  `role` ENUM('admin', 'staff') DEFAULT 'staff'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `rooms` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `room_number` INT NOT NULL UNIQUE,
  `floor` INT NOT NULL,
  `status` ENUM('trong', 'khach', 've_sinh') DEFAULT 'trong',
  `checkin_time` DATETIME NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `services` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `service_name` VARCHAR(100) NOT NULL,
  `price` INT NOT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `room_services` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `room_id` INT,
  `service_id` INT,
  `quantity` INT DEFAULT 1,
  `order_time` TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`room_id`) REFERENCES `rooms`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `system_logs` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `user_id` INT,
  `username` VARCHAR(50),
  `action` TEXT,
  `log_time` TIMESTAMP DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- Nạp tài khoản tối cao mặc định (Pass thô không mã hóa)
INSERT INTO `users` (username, password, fullname, role) VALUES 
('admin', '123456', 'Quản Trị Viên Tối Cao', 'admin'),
('nhanvien', '123456', 'Nhân Viên Lễ Tân', 'staff');

-- Nạp danh mục dịch vụ mẫu
INSERT INTO `services` (service_name, price) VALUES 
('Nước suối', 10000),
('Mì ly', 20000),
('Coca Cola', 15000),
('Bia Heineken', 25000);

-- Tự động khởi tạo 80 phòng (Tầng 1: 101->120, Tầng 2: 201->220, Tầng 3: 301->320, Tầng 4: 401->420)
DELIMITER $$
CREATE PROCEDURE InitRooms()
BEGIN
    DECLARE i INT DEFAULT 1;
    DECLARE j INT DEFAULT 1;
    WHILE i <= 4 DO
        SET j = 1;
        WHILE j <= 20 DO
            INSERT IGNORE INTO `rooms` (room_number, floor, status) VALUES ((i * 100) + j, i, 'trong');
            SET j = j + 1;
        END WHILE;
        SET i = i + 1;
    END WHILE;
END$$
DELIMITER ;
CALL InitRooms();
DROP PROCEDURE InitRooms;
